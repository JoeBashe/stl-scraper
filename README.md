# stl-scraper - Short-Term Listings Scraper

## Requirements

- Python 3.10+
- Linux

## Installation

Clone the repo, then:

```shell
python3 -m venv env
. env/bin/activate
pip install -r requirements.txt
```

## Usage

```shell
. env/bin/activate

./stl.py "Madrid, Spain"
